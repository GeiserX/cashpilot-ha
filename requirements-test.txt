pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
# aiohttp 3.14.0 made ClientResponse.__init__ require a keyword-only
# stream_writer arg, which aioresponses 0.7.8 (latest) does not pass.
# Cap below 3.14 until aioresponses ships a compatible release.
aiohttp<3.14
aioresponses
voluptuous
